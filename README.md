# git-config

```
core.editor=code --wait
alias.ci=commit
alias.ck=checkout
alias.br=branch
alias.st=status
alias.re=reset
alias.rv=revert
alias.go=checkout -b
alias.l=log HEAD --stat --graph --name-status --pretty=format:'----------------------------%n%C(yellow)%h%C(white)%d %C(blue)%an, %Creset%ar%n%n%Cgreen%s%n'
alias.reci=commit --amend
alias.unadd=reset HEAD
alias.sm=merge --squash
alias.subpull=submodule foreach git pull origin master
```
